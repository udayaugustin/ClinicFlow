# Frontend Environment Variables

# API Backend URL
# Leave empty for same-origin deployment (frontend and backend on same domain)
# Set to backend URL for separate deployment
VITE_API_URL="https://your-backend-domain.com"

# Example for local development with separate services:
# VITE_API_URL="http://localhost:3000"
